
<div class="background-container">
  <div class="msg">
    <p>👋 </p>
    <p> Hello! Before we can recommend you to one of our advisor we need to get to know a bit more! It should take you <strong> less than 30s </strong></p>
  </div>
  <h1 class="goal"> What's your goal?</h1>
    <div class="btn-wrapper">
    <% Request::TYPES.each_with_index do |type, index| %>
      <!-- <input class="form-check-input" type="checkbox" id="inlineCheckbox3"> -->
      <!-- <label class="form-check-label" for="inlineCheckbox3"></label> -->
      <%= simple_form_for :specialty, url: specialty_requests_path, method: :post, class: '' do |f| %>
        <%= f.input :specialty, as: :hidden, input_html: { value: type } %>
        <%= f.submit type.capitalize, class: ' btn  btn-outline-warning w-80' %>
      <% end %>
    <% end %>
    </div>
</div>
